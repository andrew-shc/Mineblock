// each `u32` values correspond to each image index stored in buffer
pub struct Cube {
    top: u32,
    bottom: u32,
    left: u32,
    right: u32,
    front: u32,
    back: u32,
}

impl Cube {
    // returns the vectors for the cube
    fn vec_data() {

    }
}
